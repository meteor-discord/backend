# Meteor bot backend

Very simple because it's written in Go

## Installation

```
git clone https://github.com/meteor-discord/backend
cd backend
cp .env.example .env # and fill in the api key
go run cmd/meteor-backend/main.go
```
